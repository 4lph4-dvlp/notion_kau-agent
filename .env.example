# 복사해서 .env 로 저장하고 값을 채운다. .env 는 절대 공유/커밋하지 않는다.

# Figma → Settings → Security → Personal access tokens 에서 발급
# 필요한 스코프: File content = Read
FIGMA_TOKEN=figd_

# 파일 URL 의 figma.com/design/<FILE_KEY>/<파일명> 부분
FIGMA_FILE_KEY=
